const {
  getCanonicalDepartmentId,
  resolveUserDepartment,
  isDeptMatch
} = require('../security/departmentResolver');

module.exports = {
  getCanonicalDepartmentId,
  resolveUserDepartment,
  isDeptMatch
};
